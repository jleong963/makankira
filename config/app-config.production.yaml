# =====================================================================
# PRODUCTION profile (committed). Same shape as local; only non-secret
# literals differ. apiBaseUrl is the same-origin relative path so a
# domain change keeps working. Secrets come from GitHub Actions env in CI.
# =====================================================================

frontend:                                  # PUBLIC values compiled into the web app
  apiBaseUrl: "/api"                       # same-origin on the production domain
  googleOAuthClientId: ${GOOGLE_OAUTH_CLIENT_ID}
  inactiveTimeout: ${INACTIVE_TIMEOUT:960}
  vapidPublicKey: ${VAPID_PUBLIC_KEY}

backend:                                   # SECRET values, server-side only
  tursoDatabaseUrl: ${TURSO_DATABASE_URL}
  tursoAuthToken: ${TURSO_AUTH_TOKEN}
  sessionSecret: ${SESSION_SECRET}
  fileStorageToken: ${BLOB_READ_WRITE_TOKEN}
  resendApiKey: ${RESEND_API_KEY}
  resendFrom: ${RESEND_FROM}
  vapidPublicKey: ${VAPID_PUBLIC_KEY}
  vapidPrivateKey: ${VAPID_PRIVATE_KEY}
  vapidSubject: "mailto:reminders@makankira.app"
  cronSecret: ${CRON_SECRET}
